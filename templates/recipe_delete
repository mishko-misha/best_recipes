<form method="post" action="/recipes/{{ recipe.id }}/delete/">
{% csrf_token %}
    <p>Are you sure you want to delete the recipe "{{ recipe.name }}"?</p>
    <button type="submit">Yes, delete</button>
    <a href="/recipes/{{ recipe.id }}">No, go back</a>
</form>