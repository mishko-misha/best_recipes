<form method="post" action="/recipes/{{ recipe.id }}/delete/">
{% csrf_token %}
    <p>Are you sure you want to delete the recipe "{{ recipe.title }}"?</p>
    <button type="submit">Yes, delete</button>
    <button type="button" onclick="window.location.href='/recipes/{{ recipe.id }}/delete/'">Delete</button>
</form>